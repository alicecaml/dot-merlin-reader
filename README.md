# Dot Merlin Reader

The `dot-merlin-reader` package from the
[Merlin](https://github.com/ocaml/merlin) project, manually copied to a
separate repo to simplify compilation by Alice's tool build scripts.
